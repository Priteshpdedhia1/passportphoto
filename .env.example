# ============================================
# PASSPORT PHOTO GENERATOR - ENVIRONMENT SETUP
# ============================================

# ---------------
# FRONTEND (.env)
# ---------------

# Google OAuth Client ID (OPTIONAL)
# Leave empty to run in LOCAL MODE (photos will be downloaded instead of saved to Drive)
# To enable Google Drive integration:
#   1. Go to https://console.cloud.google.com/
#   2. Create a new project
#   3. Enable Google Drive API
#   4. Create OAuth 2.0 Client ID (Web application)
#   5. Add authorized JavaScript origins: http://localhost:3000
#   6. Copy the Client ID and paste below
REACT_APP_GOOGLE_CLIENT_ID=

# Backend API URL (automatically configured in deployed environment)
REACT_APP_BACKEND_URL=http://localhost:8001

# ---------------
# BACKEND (.env)
# ---------------

# MongoDB Connection
MONGO_URL=mongodb://localhost:27017
DB_NAME=passport_photos_db

# Google Drive Folder ID (OPTIONAL)
# Leave empty to save photos to the root of user's Drive
# To use a specific folder:
#   1. Create a folder in Google Drive
#   2. Open the folder
#   3. Copy the folder ID from the URL: https://drive.google.com/drive/folders/FOLDER_ID_HERE
#   4. Paste the ID below
GOOGLE_FOLDER_ID=

# CORS Origins (comma-separated list)
CORS_ORIGINS=*

# Backend URL (for constructing download URLs in local mode)
BACKEND_URL=http://localhost:8001

# ============================================
# SETUP INSTRUCTIONS
# ============================================

# QUICK START (No Google Drive):
# 1. Copy this file to /app/frontend/.env and /app/backend/.env
# 2. Leave GOOGLE_CLIENT_ID empty
# 3. App will run in local mode - photos will be downloaded

# FULL SETUP (With Google Drive):
# 1. Follow the Google Cloud Console setup steps above
# 2. Add your GOOGLE_CLIENT_ID to frontend/.env
# 3. (Optional) Add GOOGLE_FOLDER_ID to backend/.env
# 4. Restart both frontend and backend servers

# MONGODB:
# - Default connection: mongodb://localhost:27017
# - For MongoDB Atlas: Replace with your connection string
# - Database: passport_photos_db

# TROUBLESHOOTING:
# - Error "redirect_uri_mismatch": Add http://localhost:3000 to authorized origins
# - Error "Access blocked": Add your email to test users in OAuth consent screen
# - Photos not saving to Drive: Check browser console for errors and verify token
